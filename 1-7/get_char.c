#include <stdio.h>

main()
{
    /*
    int currentChar;
    while(1)
    {
        currentChar = getchar();
        if(currentChar == EOF)
        {
            printf("EOF == %d\n", currentChar);
            break;
        }
    }
    */
    printf("EOF == %d\n", EOF);
}
